## Operate API Field Alignment

The operate API client now uses `agentInstructions` instead of the deprecated `nextAction` field, aligning with the MCP server's normalized field naming where all tools consistently use `agentInstructions` for agent behavior instructions.
